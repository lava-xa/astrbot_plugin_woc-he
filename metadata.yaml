name: astrbot_plugin_at_info # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: /woche @ 信息输出 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 监听白名单 QQ 群聊中的 /woche @ 消息，并输出被 @ 用户的 QQ 号和昵称 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: lava、哈基麦的屁股 # 作者
# repo:  # 插件的仓库地址
